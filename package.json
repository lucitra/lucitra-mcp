{
  "name": "lucitra-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Lucitra's local-dev MCP tool collection. Single entrypoint that aggregates browser, desktop, and native macOS automation tools.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "dev": "pnpm --filter @lucitra/mcp dev"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@9.0.0"
}
